
public class Student {

	private String nume;
	private int varsta;
	
	@Override
	public int hashCode()
	{
		String s = String.valueOf(this.nume.charAt(0)).toLowerCase();
		return s.hashCode()+this.varsta; 
	}
}
